import { Routes } from '@angular/router';
import { Home } from './components/home/home';

export const routes: Routes = [

    {path: "", component:Home}
    // Configurar la ruta {path: "login", component: Login}
    // etc asi misma logica de las rutas

];
